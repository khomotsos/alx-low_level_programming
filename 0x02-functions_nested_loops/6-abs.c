#include "main.h"
#include <stdio.h>

/**
 * _abs - compute the absolute value of an interge
 * @t: The interge to be computed
 * Return: Absolute value
 */
int _abs(int t)
{
    if (t >= 0)
	  return (t);
    else
	    return (-t);
}
